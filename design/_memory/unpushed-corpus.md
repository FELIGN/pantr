---
name: unpushed-corpus
description: claude-config is six commits ahead of origin as of 2026-08-24, and pushing it is Pablo's call, not an agent's
metadata:
  type: project
---

**As of 2026-08-24, `~/Dev/claude-config` is six commits ahead of `origin/main`.** The newest is
`b3d3f13`, the corpus entry a dispatched `mathematician` wrote on the de Casteljau floor when the
triangle stores narrow. The other five predate that session.

**Do not push it.** Ruled 2026-08-24. The corpus is shared across machines and collaborators, and
five of the six commits are of unknown provenance from an agent's point of view: half-finished,
deliberate, or waiting on something. Pushing another author's work without knowing its state is
worse than leaving it.

**What this costs, so the trade is visible:** an entry that exists only on this machine is not
there for the next agent that needs it, and that case already occurred once. The mathematician
that refuted a bound in the root-finding port found the corpus already held the relevant lever
and had to add the missing half itself.

Surface it again if it grows, but the decision stays Pablo's.
